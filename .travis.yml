language: generic
env: SWIFT_VERSION=4.2-DEVELOPMENT-SNAPSHOT-2018-06-02-a

matrix:
  include:
    - os: linux
      sudo: required
      dist: trusty
    - os: osx
      osx_image: xcode9

install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - make
  - make test
